# inbox 使用说明

`inbox/` 是这个工作树的收件箱，适合先把零散输入接住，再决定它应该落到哪里。

## 适合先丢进来的内容

- 临时需求
- 会议纪要草稿
- 用户反馈摘录
- 待核实的问题
- 一时还没想好归属的材料

## 处理规则

1. 新条目先按 `YYYY-MM-DD-topic.md` 命名。
2. 一旦进入正式推进，就把内容迁到对应项目的 `docs/`、项目目录或 `knowledge-base/`。
3. `inbox/` 只存待分流内容，不做长期归档。

## 模板

- `_template.md`
